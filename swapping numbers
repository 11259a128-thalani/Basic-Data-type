(a)Swapping numbers(call by value)
#include sstdio.h>
void swap(int a, int b) {
  int temp = a;
  a= b;
  b= temp;
  printf(Inside function (Call by Value): a=%d b=%d\n", a, b);
 }
 int main() {
    intx= 10, y=20;
    swap(x, y);
    printf("Outside function: x=%d y=%d\n", x, y);
    return 0;
}


OUTPUT:

Inide function (call by value) : a=20 b=10
outside function               : x=10 y=20


(b)Swapping Numbers (Call by Reference)
 #include ^stdio.h>
void swap(int *a, int *b){
  int temp =*a;
  *a=*b;
  *b=*temp;
 }
int main() {
  int x= 10, y= 20;
       0012, 1002
 swap(&x, &y);
 printf("After swap: x=%d y=%d)n", x,.y);
 return 0;
}



OUTPUT:


Inside function(call by reference) : a=10 b=20
outside function                   : x=20 y=10

