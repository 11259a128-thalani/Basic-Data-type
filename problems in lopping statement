(il Sum of Series using For Loon
#include <stdio.h>
int main() {
    int n, sum = 0;
    printf("Enter n:");
    scanf ("%d", &n);
    for(int i= 1;i<=n;i++)
        sum += i;
    printf("Sum of series = %d\n", sum) ;
    return 0;


    OUTPUT:


    Enter n       =13
    sum of series =91

    
(ii) Sum of Series using While Loop
#include <stdio.h>
int main() {
    int n, i=l, sum = O; 
    printf ("Entet n: "");
    scanf ("음d", &n);
    while(i <=n) {
    sum += i;
    i++;
}
    printf("Sum of series = %d\n", sum);
    return 0;
    }


    OUTPUT:
    Null point assigment 
    

  
(iii) Fibonacci Series
#include <stdio.h>
int main() (
   int n, a = 0, b =1, next;
   printf("Enter number of terms:");
   scanf ("%d", &n);
   printf ("Fibonacci Series:");
   for(int i = l; i <=n; i++){
       printf ("%d ", a);
       next =a + b;
       a = b;
       b = next;
  }      
    return 0;
} 


OUTPUT:
enter number of terms:20
fibonacci series     :0112358132134558914423337610987159725844181

(iv) Check Prime Number 
#include <stdio.h>
int main() 
{
    int n, flag = 0;
    printf("Enter a number: ");
    scanf ("%d", &n);
for(int i = 2;i<=n/2；i++) {
    if (n % i== 0){
     flag = 1;
     break;
   } 
 }  
if (flag == 0)
    printf("%d is a Prime number\n", n);
else
    printf ("%d is not a Prime number\n", n);
return 0;
}


OUTPUT:


Enter a prime number :10
10 is not prime number
