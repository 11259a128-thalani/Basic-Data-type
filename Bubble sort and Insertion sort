(a) Bubble Sort
#include <stdio.h>
int main() {
     int n, i, j, temp;
     printf ("Enter number of elements:");
     scanf ("%d", &n);
     int a[n];
     printf("Enter elements:")
     for(i =0;i<n; i++)
          scanf ("%d", &a[i]);
for(i = 0;i<n-l;i++)
    for(j =0;j<n-i-l;j++)
        if (a[i]>a[i+1]); {
            temp = a[i];
            a[j] = a[j+1];
            a[j+1] = temp;
         }   
printf ("Sorted array:");
for(i = 0;i<n;i++)
    printf ("%d ", a[i]);
  return 0;
} 


output:


enter number of elements:5
enter elements :4 5 6 7 8 
sorted arrays  :4 5 6 7 8


   
